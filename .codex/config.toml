# Default to the cheaper package-local profile for routine work.
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"

[features]
multi_agent = true

[profiles.package_local]
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"

[profiles.contracts_review]
model = "gpt-5.4"
model_reasoning_effort = "medium"

[profiles.integration_review]
model = "gpt-5.4"
model_reasoning_effort = "high"

